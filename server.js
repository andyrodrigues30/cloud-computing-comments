var express = require("express");
var app = express();

// TODO: [GET] /comments - all comments
// TODO: [POST] /comment - new comment - {message, creator, timestamp}

app.listen(3000, () => {
    console.log("Server running on port 3000");
});